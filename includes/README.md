config.php — for your PHP (backend)

This file is used by your backend (PHP) scripts,
like your insert_customers.php, update_customers.php, etc.

It usually contains:

- Database connection details (host, username, password)

- Base URLs (optional)

- Global constants or settings used by PHP files